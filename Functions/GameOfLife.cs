using System;

public class GameOfLife
{
    //public static int[] helpToSearch(int myX, int myY, int row, int col)
    //{
    //    Dictionary<string, int> borders = new Dictionary<string, int>();
    //    int retXlow, retXhigh, retYlow, retYhigh;
    //    retXlow = myX - 1;
    //    retXhigh = myX + 1;
    //    retYlow = myY - 1;
    //    retYhigh = myY + 1;

    //    if (myX - 1 < 0)
    //        retXlow = 0;
    //    if (retXhigh <= col)
    //        retXhigh = col - 1;
    //    if (retYhigh <= row)
    //        retYhigh = col - 1;
    //    if (retYlow < 0)
    //        retYlow = 0;
    //    borders.Add("xLow", retXlow);
    //    borders.Add("xHigh", retXhigh);
    //    borders.Add("yLow", retYlow);
    //    borders.Add("yHigh", retYhigh);


    //}
}
